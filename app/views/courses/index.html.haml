.content
  .row
    .col-md-12
      - if @courses.present?
        - @courses.each do |course|
          .col-md-3
            .thumbnail
              = course.title.humanize
              %br
              = image_tag course.picture.admin_thumb
        = paginate @courses
      - else
        %h2 No courses
