%li{"data-move-url" => move_users_course_lesson_path(lesson), "data-position" => lesson.position, "data-role" => "lesson#{lesson.id}"}
  .col-md-4
    %h4 = lesson.title
    = image_tag lesson.picture.thumb
  .col-md-8
    = lesson.description
  .col-md-8
    = lesson.lecture_notes
  .col-md-8
    = lesson.home_task